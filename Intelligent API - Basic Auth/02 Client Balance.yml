info:
  name: 02 Client Balance
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}v1/client/balance"
  auth:
    type: basic
    username: "{{basicAuthClientId}}"
    password: "{{basicAuthClientSecret}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
