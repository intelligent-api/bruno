info:
  name: 02 Summarize Web Page
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}v1/scraper/summarize"
  body:
    type: json
    data: |-
      {
          "url": "https://fullcycle.tech"
      }
  auth:
    type: basic
    username: "{{basicAuthClientId}}"
    password: "{{basicAuthClientSecret}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
