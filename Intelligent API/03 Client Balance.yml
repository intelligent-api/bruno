info:
  name: 03 Client Balance
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}v1/client/balance"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
