info:
  name: 01 Supported Languages
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}v1/text/sentiment/supportedlanguages"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
