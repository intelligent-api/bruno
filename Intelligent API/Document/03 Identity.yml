info:
  name: 03 Identity
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}v1/document/identity"
  headers:
    - name: X-Correlation-Id
      value: dfe8e127-14aa-42e5-8397-118ebd4594b3
      disabled: true
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
